export interface Session {
  apiKey: string
  sessionId: string
  token: string
  focusStreamId: string
  layout: string
}
export interface ContactModel {
  id:string;
  name: string;
  position: number;
  phone: number;
  email: string;
}
